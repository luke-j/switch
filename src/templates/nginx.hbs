server {
    {{#each context.lines}}
        {{this}};
    {{/each}}

    {{#each context.contexts}}
        {{this.type}} {{this.directive}} {
        {{#each this.lines}}
            {{this}};
        {{/each}}
        }

    {{/each}}
}

